@model Restauracja.Models.PrzyjmowaneDane.RestauracjaModel

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/DetailsrestauracjaStyl.css" />
</head>

<body>
    <div class="restaurant-container">
        <!-- Nagłówek -->
        <div class="restaurant-header">
            Restauracja: <strong>@Model.Nazwa</strong>
        </div>

        <!-- Szczegóły -->
        <div class="restaurant-details">
            <form asp-controller="Restauracja" asp-action="PrzeprowadzEdycje" asp-route-id="@Model.Id" method="post">
                @if (string.IsNullOrEmpty(Model.Zdjecie))
                {
                    <div style="float:right;">
                        <img src="https://via.placeholder.com/300x200" alt="Zdjęcie restauracji">
                    </div>
                }
                else
                {
                    <div style="float:right;">
                        <img src="data:image/jpeg;base64,@Model.Zdjecie" alt="Zdjęcie restauracji">
                    </div>
                }
                <!-- Nazwa -->
                <div class="form-group">
                    <label for="Nazwa">Nazwa:</label>
                    <input type="text" id="Nazwa" name="Nazwa" value="@Model.Nazwa">
                </div>

                <!-- Opis -->
                <div class="form-group">
                    <label for="Opis">Opis:</label>
                    <input type="text" id="Opis" name="Opis" value="@Model.Opis"></input>
                </div>

                <!-- Zdjecie -->
                <div class="form-group">
                    <label for="Zdjecie">Wybierz zdjęcie:</label>
                    <input type="file" id="PlikZdjecia" accept="image/*" value="@Model.Zdjecie"/>
                </div>

                <!-- TypLokalu -->
                <div class="form-group">
                    <label for="TypLokalu">Typ Lokalu:</label>
                    <input type="text" id="TypLokalu" name="TypLokalu" value="@Model.TypLokalu">
                </div>

                <!-- TypRestauracji -->
                <div class="form-group">
                    <label for="TypRestauracji">Typ Restauracji:</label>
                    <input type="text" id="TypRestauracji" name="TypRestauracji" value="@Model.TypRestauracji">
                </div>

                <!-- CzyDostawa -->
                <div class="form-group">
                    <label for="CzyDostawa">Czy Dostawa:</label>
                    <input type="checkbox" id="CzyDostawa" name="CzyDostawa" value="true" @(Model.CzyDostawa ? "checked" : "")>
                </div>

                <!-- CzyDriveThru -->
                <div class="form-group">
                    <label for="CzyDriveThru">Czy Drive-Thru:</label>
                    <!-- Checkbox -->
                    <input type="checkbox" id="CzyDriveThru" name="CzyDriveThru" value="true" @(Model.CzyDriveThru ? "checked" : "")>
                </div>

                <!-- CzyParkinPrzyRestauracji -->
                <div class="form-group">
                    <label for="CzyParkinPrzyRestauracji">Czy Parking Przy Restauracji:</label>
                    <input type="checkbox" id="CzyParkinPrzyRestauracji" name="CzyParkinPrzyRestauracji" value="true" @(Model.CzyParkinPrzyRestauracji ? "checked" : "")>
                </div>

                <!-- CzyMozliwaRezerwacja -->
                <div class="form-group">
                    <label for="CzyMozliwaRezerwacja">Czy Możliwa Rezerwacja:</label>
                    <input type="checkbox" id="CzyMozliwaRezerwacja" name="CzyMozliwaRezerwacja" value="true" @(Model.CzyMozliwaRezerwacja ? "checked" : "")>
                </div>

                <!-- CzyImprezyOkolicznosciowe -->
                <div class="form-group">
                    <label for="CzyImprezyOkolicznosciowe">Czy Imprezy Okolicznościowe:</label>
                    <input type="checkbox" id="CzyImprezyOkolicznosciowe" name="CzyImprezyOkolicznosciowe" value="true" @(Model.CzyImprezyOkolicznosciowe ? "checked" : "")>
                </div>

                <!-- CzySalaOkolicznosciowa -->
                <div class="form-group">
                    <label for="CzySalaOkolicznosciowa">Czy Sala Okolicznościowa:</label>
                    <input type="checkbox" id="CzySalaOkolicznosciowa" name="CzySalaOkolicznosciowa" value="true" @(Model.CzySalaOkolicznosciowa ? "checked" : "")>
                </div>

                <!-- GodzinaOtwarcia -->
                <div class="form-group">
                    <label for="GodzinaOtwarcia">Godzina Otwarcia:</label>
                    <input type="time" id="GodzinaOtwarcia" name="GodzinaOtwarcia" value="@Model.GodzinaOtwarcia">
                </div>

                <!-- GodzinaZamkniecia -->
                <div class="form-group">
                    <label for="GodzinaZamkniecia">Godzina Zamknięcia:</label>
                    <input type="time" id="GodzinaZamkniecia" name="GodzinaZamkniecia" value="@Model.GodzinaZamkniecia">
                </div>

                <!-- CzasOtwarcia -->
                <div class="form-group">
                    <label for="CzasOtwarcia">Czas Otwarcia:</label>
                    <input type="time" id="CzasOtwarcia" name="CzasOtwarcia" value="@Model.CzasOtwarcia">
                </div>
                <div class="card-footer d-flex justify-content-end gap-2">
                    <button type="submit" class="btn btn-success mt-3">Zapisz</button>
                </div>
            </form>
        </div>
        <div class="restaurant-footer">
            © @DateTime.Now.Year Twoja Restauracja | Wszystkie prawa zastrzeżone
        </div>
    </div>
</body>

